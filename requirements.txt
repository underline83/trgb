# TRGB Gestionale — requirements (v1.4)
# Dipendenze backend FastAPI

fastapi
uvicorn[standard]
python-jose[cryptography]
passlib[bcrypt]

# Excel / Import
pandas
openpyxl
pyxlsb

# Upload files
python-multipart

# PDF generator
weasyprint

# PDF reader/parser (LUL cedolini)
pdfplumber

# HTML templating
jinja2

# Word / DOCX
python-docx

# Date utilities
python-dateutil

# Variabili d'ambiente (.env)
python-dotenv

# HTTP client per API esterne (Fatture in Cloud)
httpx

#Grafici
