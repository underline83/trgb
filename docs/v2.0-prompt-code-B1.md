# Prompt per Code — Fase B.1 v2.0 (query /uscite JOIN)

**Contesto**: stiamo facendo il refactoring v2.0 "CG come aggregatore" del modulo Controllo di Gestione. La Fase A è chiusa (mig 055/056/057 applicate, 43 fatture rateizzate backfillate). Adesso dobbiamo riscrivere la query dell'endpoint `/uscite` per usare `fe_fatture` come fonte di verità per i campi di pianificazione finanziaria (date, IBAN, modalità pagamento), mantenendo `cg_uscite` come indice di workflow.

La query nuova è già stata **progettata, testata funzionalmente e benchmarkata** su DB locale. Tu devi solo integrarla nel router, preservando la retrocompatibilità del payload.

## File di riferimento (leggili prima di toccare qualsiasi cosa)

1. `docs/v2.0-query-uscite.sql` — query nuova completa, decisioni di design, benchmark, scenari verificati
2. `docs/v2.0-decisioni.md` §A1, §A3, §B1, §B2 — perché i tre campi data, perché CASE-in-SELECT
3. `docs/v2.0-roadmap.md` §4 "Fase B" — contesto della fase
4. `app/routers/controllo_gestione_router.py` righe **764-883** — la funzione `get_uscite` attuale da modificare

## Obiettivo concreto

Sostituire l'implementazione della funzione `get_uscite` in `controllo_gestione_router.py` con la nuova versione basata sulla query di `v2.0-query-uscite.sql` §2.

**Non toccare altre funzioni**. Non toccare `get_fatture_senza_scadenza`, non toccare `update_scadenza`, non toccare niente che non sia `get_uscite` e l'arricchimento Python che la segue.

## Modifiche richieste

### 1. Aggiungere parametro `includi_rateizzate`

Firma della funzione attuale:

```python
def get_uscite(
    stato: Optional[str] = Query(default=None),
    fornitore: Optional[str] = Query(default=None),
    da: Optional[str] = Query(default=None, description="Data scadenza da (YYYY-MM-DD)"),
    a: Optional[str] = Query(default=None, description="Data scadenza a (YYYY-MM-DD)"),
    ordine: str = Query(default="scadenza_asc"),
    current_user=Depends(get_current_user),
):
```

Aggiungi:

```python
    includi_rateizzate: bool = Query(default=False, description="Se True mostra anche le fatture rateizzate"),
```

### 2. Sostituire la query SQL

La query attuale (righe 808-828) va sostituita integralmente con la versione di `docs/v2.0-query-uscite.sql` §2. Copia **fedelmente** quella query, inclusi alias e CASE. Non ottimizzarla, non riformattarla creativamente: il design è frutto di 5 iterazioni e benchmark, ogni pezzo è motivato.

**Attenzione ai dettagli**:

- La WHERE clause della query deve combinare il filtro rateizzate (sempre presente) con i filtri dinamici (costruiti come prima). Pattern:
  ```python
  where = ["(:includi_rateizzate = 1 OR (f.rateizzata_in_spesa_fissa_id IS NULL AND u.stato <> 'RATEIZZATA'))"]
  params = {"includi_rateizzate": 1 if includi_rateizzate else 0}

  if stato:
      where.append("u.stato = :stato")
      params["stato"] = stato
  # ... etc
  ```
  Usa **parametri nominali** (`:nome`) invece di `?` posizionali, perché `includi_rateizzate` va referenziato dentro la clausola fissa.

- Il mapping `ordine_map` deve puntare a `data_scadenza_effettiva` invece di `u.data_scadenza` per le opzioni di ordinamento per scadenza:
  ```python
  ordine_map = {
      "scadenza_asc":  "data_scadenza_effettiva ASC NULLS LAST",
      "scadenza_desc": "data_scadenza_effettiva DESC NULLS LAST",
      "importo_asc":   "u.totale ASC",
      "importo_desc":  "u.totale DESC",
      "fornitore":     "u.fornitore_nome ASC",
      "data_fattura":  "u.data_fattura DESC",
  }
  ```

- Il filtro range data (`da`/`a`) deve puntare a `data_scadenza_effettiva`, non a `u.data_scadenza`:
  ```python
  if da:
      where.append("COALESCE(f.data_effettiva_pagamento, f.data_prevista_pagamento, u.data_scadenza, f.data_scadenza) >= :da")
      params["da"] = da
  if a:
      where.append("COALESCE(f.data_effettiva_pagamento, f.data_prevista_pagamento, u.data_scadenza, f.data_scadenza) <= :a")
      params["a"] = a
  ```
  Sì, è una duplicazione del COALESCE: SQLite non permette di referenziare alias del SELECT nella WHERE. Non provare a wrapparla in CTE, il benchmark ha mostrato che è inutile.

- Il filtro `stato` resta su `u.stato` (NON sulla CASE computata). Motivo: in v2.0 Fase B la transizione non è ancora completa — `u.stato` resta la fonte per i filtri utente, mentre la CASE in SELECT è solo per il display. Quando arriverà Fase C, l'allineamento avverrà sulle nuove rateizzazioni via `UPDATE cg_uscite SET stato='RATEIZZATA'`, e per le fatture pagate via `data_effettiva_pagamento`. Per ora: filtri = vecchia logica, display = nuova logica.

### 3. Arricchimento Python dopo la fetch

Il loop attuale che itera le righe e arricchisce con label va **preservato** e **esteso**. Le aggiunte:

a) **Rename dei campi computati al nome "pubblico"**: il frontend legge `row["data_scadenza"]` e `row["stato"]` — la query nuova restituisce queste come alias diverse per evitare conflitti. Fai questo rename:

```python
for r in uscite:
    row = dict(r)
    # v2.0: rimappa i campi computati ai nomi pubblici del payload
    row["data_scadenza"] = row.pop("data_scadenza_effettiva")
    # row["stato"] arriva già con il nome giusto dalla CASE del SELECT
    # row["stato_cg"] resta accessibile se serve (raw da cg_uscite)
    # row["data_scadenza_cg"] resta accessibile (raw da cg_uscite)
    ...
```

b) **Nuovi campi additivi** che il frontend può ignorare finché non implementa le Fasi D/E:
   - `row["rateizzata_in_spesa_fissa_id"]` (già presente dalla query, nessuna azione)
   - `row["data_prevista_pagamento"]`, `row["data_effettiva_pagamento"]` (già presenti)
   - `row["is_rateizzata"]` (0/1, utile per badge)
   - `row["data_scadenza_xml"]` (la scadenza XML originale, per audit)
   - `row["iban_beneficiario_effettivo"]`, `row["modalita_pagamento_effettiva"]` (già presenti)

c) **Arricchimento modalità pagamento label**: attualmente legge `mp_xml` o `mp_fornitore`. Ora deve preferire `modalita_pagamento_effettiva` (che già ha il fallback completo applicato dal SQL):
   ```python
   mp = row.get("modalita_pagamento_effettiva") or row.get("mp_xml") or row.get("mp_fornitore")
   row["modalita_pagamento_label"] = MP_LABELS.get(mp, mp) if mp else None
   row["modalita_pagamento_codice"] = mp
   ```

d) Tutto il resto (scadenza_fonte, metodo_pagamento_label, sf_tipo_label, etc.) resta identico.

### 4. Riepilogo finale (blocco con `totale_da_pagare`, etc.)

Il blocco riepilogo (righe 857-866) va **riscritto leggermente** perché lo `stato` adesso è il valore CASE computato dalla query, non più `u.stato` raw. In pratica non cambia niente nei calcoli perché si basa su `row["stato"]`, che adesso è il normalizzato.

**Verifica**: dopo il refactoring, confronta i totali riepilogo con un DB di prova. Devono scendere del valore delle rateizzate (nel sandbox test: delta ~€56k129).

## Test che DEVI fare prima di dire "fatto"

1. Il backend parte senza errori (`uvicorn main:app --reload` in sandbox).
2. `GET /contabilita/cg/uscite` risponde 200, il JSON ha le stesse chiavi del payload v1.7.1 + quelle nuove.
3. `GET /contabilita/cg/uscite?includi_rateizzate=true` restituisce 43 righe in più rispetto al default (nel dataset sandbox).
4. `GET /contabilita/cg/uscite?stato=DA_PAGARE&fornitore=METRO` — controlla che NON contenga nessuna delle 32 fatture Metro rateizzate.
5. `GET /contabilita/cg/uscite?da=2025-06-01&a=2025-06-30` — tutte le righe devono avere `data_scadenza` in quel range.
6. `GET /contabilita/cg/uscite?ordine=scadenza_desc` — la prima riga deve avere `data_scadenza` più recente dell'ultima.

## Cosa NON devi fare

- NON toccare altre funzioni del router.
- NON creare nuove migrazioni (gli indici ci sono già).
- NON fare `git commit` (gestisce Marco con `./push.sh`).
- NON modificare il frontend (quello è Fase E).
- NON provare UNION ALL (Marco e Cowork hanno già scartato l'opzione dopo benchmark).
- NON rimuovere campi dal payload: tutti i campi di cg_uscite.* devono ancora essere presenti (retrocompatibilità).
- NON ottimizzare "creativamente" la query: il design è testato, ogni pezzo è motivato in `v2.0-query-uscite.sql`.

## Testo del commit da passare a Marco

Quando hai finito e testato tutto, lascia un messaggio con questo testo esatto per Marco:

```
./push.sh "feat(CG v2.0b): query /uscite come vista JOIN fe_fatture + filtro rateizzate"
```

## Note finali

- Il benchmark dice ratio 1.32x rispetto a v1.7.1: ~3ms di differenza su 2045 righe. Non preoccuparti della performance, non è un problema.
- Il DB locale sandbox ha 43 fatture flaggate come `rateizzata_in_spesa_fissa_id` NOT NULL. Se vuoi verificare qualcosa usa `python3 -c "import sqlite3; ..."` sul file `app/data/foodcost.db`.
- Se qualcosa in questo prompt ti sembra ambiguo, FERMATI e chiedi a Marco invece di inventare. La query è delicata.
