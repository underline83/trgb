# 11 — Ricognizione DELTA audit 2026-06-12 → stato al 2026-07-10

**Data ricognizione:** 2026-07-10 · **Versione live:** 5.32 · **Commit HEAD/VPS:** `ead338ad` (allineati)
**Baseline:** audit 2026-06-12 (commit `1f5f9c17`, v5.24) — 110 finding, voto 63/100
**Metodo:** ri-verifica dei finding contro il codice attuale (grep sul repo) + 3 probe HTTP live dal cloud (Banca, iPratico, Swagger, header). Le voci infra-VPS che richiedono `ssh trgb` (sshd, porte) non sono state ri-verificate live in questa sessione e restano allo stato dichiarato dalla Sessione 1 del 12/06.

---

## 0. Sintesi in una riga

**In 28 giorni si è chiusa solo la Sessione 1 (parziale).** Dei 110 finding risultano **chiusi 4** (1 CRIT + 3 HIGH), **aperti ~106**. I 22 commit dal 13/06 sono tutti lavoro-feature (vini calice, CG rateizzazioni RC.1/RC.3, Batch pagamenti BP, Statistiche 1.2, hotfix backup, turni "Mese intero"): **nessuno tocca un finding dell'audit**. Due voci sono anzi **peggiorate** (vedi §4).

---

## 1. Cosa è stato CHIUSO (verificato questa sessione)

| ID | Sev | Finding | Prova 2026-07-10 |
|---|---|---|---|
| **A1-01** | CRIT | Modulo Banca senza auth | ✅ **LIVE 401** su `GET /banca/movimenti` senza token. `banca_router.py:44 dependencies=[Depends(get_current_user)]`. Deployato (v5.32). |
| **A1-02** | HIGH | iPratico senza auth | ✅ **LIVE 401** su `/vini/ipratico/stats`. `ipratico_products_router.py:36 dependencies=`. |
| **A6-13** | HIGH | sshd root+password | 🟡 dichiarato FATTO il 12/06 (memoria Sessione 1). **Non ri-verificato live** questa sessione (serve `ssh trgb` dal Mac). |
| **A6-12** | HIGH | Porte 3389/9000/9443 esposte | 🟡 dichiarato FATTO il 12/06 (Portainer ribindato 127.0.0.1, xrdp off). **Non ri-verificato live**. |

> **Azione consigliata:** una passata `ssh trgb` (`ss -tlnp` + `grep -E 'PermitRootLogin|PasswordAuthentication' /etc/ssh/sshd_config`) per riconfermare A6-12/A6-13, dato che sono passati 28 giorni.

---

## 2. Il residuo della Sessione 1 — MAI CHIUSO (punto 5)

Erano i due CRIT di piattaforma. **Entrambi ancora aperti nel codice:**

| ID | Sev | Stato oggi | Evidenza |
|---|---|---|---|
| **A9-01** | 🔴 CRIT | APERTO | `047_prestiti_bpm.py` e `048_cg_piano_rate.py` **senza `TRGB_SPECIFIC`** (grep = 0). Ogni DB nuovo eredita i prestiti BPM reali di Marco. |
| **A9-02** | 🔴 CRIT | APERTO | `app/core/config.py:4` → `SECRET_KEY = os.getenv("SECRET_KEY", "trgb_secret_key_2025")` invariato. Nessun fail-loud. |

Sono i due finding a effort **S** con l'impatto più alto rimasto: fixarli chiude la Sessione 1 e i 2 CRIT residui in poche righe.

---

## 3. Cosa è ancora APERTO (campione verificato oggi)

Tutti confermati aperti nel codice / live:

**Sicurezza (A1/A6)**
- A1-04 login PIN senza rate-limit/lockout — grep `slowapi|limiter|lockout` su auth = 0. APERTO.
- A1-05 `/foodcost/ingredienti` senza auth — nessun `get_current_user`. APERTO.
- A1-06 path traversal upload `admin_finance.py:223` (`{file.filename}` non sanitizzato). APERTO.
- A1-07 zero gate di ruolo su buste-paga/PII (il campo `is_amministratore` è un flag del dipendente, non un controllo sul chiamante). APERTO.
- A1-08 7 endpoint download con `?token=`. APERTO.
- **A6-06 Swagger `/docs` — LIVE 200** oggi. APERTO.
- **A6-09 header sicurezza — LIVE assenti** su app.tregobbi.it (`server: nginx/1.24.0` esposto). APERTO.

**Dati (A2)**
- A2-01 runner registra sempre in `schema_migrations` anche gli skip cross-DB (`migration_runner.py:apply_migration`). APERTO.
- A2-02 zombie `vini_magazzino` ricreata a ogni boot (`vini_magazzino_db.py:118`). APERTO.
- A2-03 / A7-02 **nessun indice `fe_righe(fattura_id)`** (grep migrazioni = 0). APERTO — il quick-win a più alto ROI dell'audit, ancora non fatto.
- A2-04 65+57 FK orfane, helper FK non unificato. APERTO.

**Backend/Perf (A3/A7)**
- A3-01 `stati_pagamento.py` **ancora non importato** da nessuno; whitelist stati hardcoded nel CG (`PAGATO_MANUALE` ×60 in controllo_gestione_router.py). APERTO — vedi §4, peggiorato.
- A3-02 trio `admin_finance_*` morto (non importato). APERTO.
- A3-03 `liquidita_service` morto ma importato a `controllo_gestione_router.py:23`. APERTO.
- A3-08 / A7-03 `run_all_checks` + `trigger_scheduler` sincroni in `/dashboard/home:1519-1528` (nessun BackgroundTasks). APERTO.
- A3-09 `except: pass` sul saldo contanti (7 except nel file). APERTO.
- A7-01 / A4-01 / A6-03 Vite dev server in prod (nessun `npm run build` in push.sh). APERTO.
- A7-06 elenchi senza paginazione. APERTO.

**Architettura (A5)**
- A5-01 / A3-05 i 4 router (`vini_anagrafiche`, `vini_v2`, `banca_carta`, `piatti_giorno`) **ancora fuori da ogni module.json** (grep = 0). `module_loader` ancora `return True` di default, nessun warning al boot. APERTO.
- A5-05 `turni_service:655` importa ancora un router di cassa. APERTO.

**Prodotto (A9)**
- A9-03 `frontend/.env.production` → `VITE_API_BASE_URL=https://trgb.tregobbi.it`, `api.js` senza fallback a `location.origin`. APERTO.
- A9-05 `backup_db.sh:41` / `check_backup_health.sh:35` `PROJECT_DIR="/home/marco/trgb/trgb"` hardcoded. APERTO.
- A9-06 push.sh -l: 4 punti hardcoded. APERTO.
- A6-08 watchdog con `DBS=(...)` lista fissa di 10 DB vs discovery dinamica di backup_db.sh. APERTO.

**Frontend/Docs (A4/A8)**
- A4-02 fetch raw: **60** occorrenze oggi (erano 57 — vedi §4). APERTO/peggiorato.
- A4-03 `CambioPIN.jsx:34` `/auth/users` ancora senza slash. APERTO.
- A8-01 `modulo_task_manager.md` / `modulo_haccp.md` inesistenti. APERTO.
- A8-03 `annulli_resi` assente da `modulo_vendite.md`. APERTO.
- A8-05 **roadmap.md ferma al 2026-05-19 — ora stantia da 52 giorni.** APERTO/peggiorato.
- A8-06 CLAUDE.md dichiara ancora M.B/M.E "DA FARE" (riga 130,132). APERTO.

> I LOW non sono stati ri-verificati uno per uno: il `git log` dal 13/06 non contiene alcun commit di cleanup/dead-code/docs-catchup, quindi si assumono **tutti aperti** salvo prova contraria al tocco del file.

---

## 4. Regressioni e nuovo debito dal 13/06

1. **A3-01 peggiorato per esposizione.** Dal 13/06 sono arrivati **RC.1/RC.3 (auto-close rateizzazioni)** e **BP.1-4 (Batch pagamenti)** — codice che manipola pesantemente stati/`cg_uscite` — mentre la SSoT `stati_pagamento.py` resta scollegata. Ogni nuova feature ha aggiunto altri punti alla whitelist hardcoded che G.8 doveva eliminare. Il rischio del finding è cresciuto, non calato.
2. **A4-02 da 57 → 60 fetch raw**: il debito "gestione 401 a mano" si sta allargando invece di ridursi.
3. **A8-05 roadmap**: da 24 a 52 giorni di stantìo; ora non riflette RC/BP/Statistiche 1.2/turni Mese.
4. **Hotfix RC.1 (5b315c64)**: SELECT su colonna inesistente `cg_uscite.numero_rata` andata a 500 in produzione ("Load failed" su Safari). È esattamente la classe di bug "query su nome colonna sbagliato" — coerente con la memoria `feedback_query_sql_nomi_colonne`. Non un finding d'audit, ma conferma che il debito CG è vivo.

---

## 5. Conteggio delta

| | CRIT | HIGH | MED | LOW | Tot |
|---|---|---|---|---|---|
| Audit 12/06 | 3 | 18 | 46 | 43 | 110 |
| ✅ Chiusi | 1 (A1-01) | 3 (A1-02, A6-12, A6-13*) | 0 | 0 | **4** |
| 🔴 Aperti | 2 | 15 | 46 | 43 | **~106** |

\* A6-12/A6-13 chiusi live 12/06, da riconfermare via ssh.

**Voto stimato invariato ~63/100** (i 4 chiusi sono di alto valore ma pochi in numero; l'area Sicurezza sale un po', Prodotto e Dati fermi).

---

## 6. Priorità consigliate per la ripartenza (riordinate su ciò che resta)

1. **Chiudere il residuo Sessione 1 (i 2 CRIT):** A9-01 (`TRGB_SPECIFIC` su 047/048 + ri-audit INSERT) + A9-02 (SECRET_KEY fail-loud + env). Effort **S**, chiude i 2 CRIT rimasti.
2. **Riconferma infra live:** `ssh trgb` per A6-12/A6-13; nell'occasione chiudere **A6-06** (`docs_url=None if IS_PROD`) e **A6-09** (3 `add_header` + `server_tokens off`) che sono ancora live-aperti e costano minuti.
3. **Sessione 3 "Igiene DB" — quick win:** **A7-02/A2-03** (indice `fe_righe`), è la singola riga a ROI più alto dell'intero audit, ancora non fatta. A seguire A2-02/A2-04 (bonifica FK).
4. **A1-04 + A6-07 "Login robusto":** invariati, il vettore brute-force su PIN è ancora scoperto.
5. **A3-01 prima che cresca ancora:** cablare `stati_pagamento.py` nel CG ora che RC/BP hanno aggiunto superficie.

Il resto del piano (Sessioni 4-11) resta valido così com'è in `10_PIANO_AZIONE.md`: nessuna sua voce è stata toccata, quindi l'ordine raccomandato non cambia.
